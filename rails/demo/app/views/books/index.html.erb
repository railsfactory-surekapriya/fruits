<h1>Listing books</h1>
 <%= link_to 'New book',new_book_path %>
<table>
  <tr>
    <th>Name</th>
    <th>Place</th>
  </tr>
 
  <% @books.each do |book| %>
    <tr>
      <td><%= book.name %></td>
      <td><%= book.place %></td>
    </tr>
  <% end %>
</table>
